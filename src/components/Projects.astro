---
const PROJECTS = [
  {
    title: "Vote Application for Santo Tomás University",
    description:
      "A vote application for the Santo Tomás University students. In order to choose the representatnt of each faculty.",
    link: "",
    github: "",
    image: "/projects/vote.jpg",
    notes:
      "This project was developed as part of my studies at Santo Tomás University as a final grade project. Hence, this project does not have a public link or a public repository.",
    tags: ["JavaScript", "Vite", "Node", "Fastify", "MongoDB"],
  },
  {
    title: "Sergio's Portfolio",
    description:
      "A personal portfolio showcasing my experience, projects and skills. Built with Astro, Tailwind CSS and Javascript. ",
    link: "https://sergios-portfolio.netlify.app/",
    github: "https://github.com/sergio200086/sergio-portfolio",
    image: "/projects/portfolioss.webp",
    notes:
      "This project was developed as part of my studies at Santo Tomás University as a final grade project. Hence, this project does not have a public link or a public repository.",
    tags: ["JavaScript", "Vite", "Node", "Fastify", "MongoDB"],
  },
];
---

{
  PROJECTS.map(({ image, title, description, tags, link, notes }) => (
    <article>
      <h1 class="text-xl font-semibold mb-3 text-indigo-200">{title}</h1>
      <p class="mb-4">{description}</p>
      <ul>
        {tags.map((tag) => (
          <li class="inline-block  bg-gray-200 text-gray-800 rounded-full px-3 py-1 text-sm font-semibold mr-2 mb-4 transition-all duration-300 hover:scale-110">
            {tag}
          </li>
        ))}
      </ul>
      <div class="relative group overflow-hidden rounded-xl mb-12 ">
        {link.length == 0 ? (
            <img
              class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
              src={image}
              alt="Project screenshot"
            />
            <div class="absolute inset-0 bg-black bg-opacity-60 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-500">
              <p class="text-white text-lg font-semibold p-20">{notes}</p>
            </div>
        ) : (
          <img
            class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
            src={image}
            alt="Project screenshot"
          />
        )}
      </div>

    </article>
  ))
}
